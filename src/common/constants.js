// used to refer to textures by their indices
export const TEXTURE_SLOTS = (gl) => [
  gl.TEXTURE0,
  gl.TEXTURE1,
  gl.TEXTURE2,
  gl.TEXTURE3,
  gl.TEXTURE4,
  gl.TEXTURE5,
  gl.TEXTURE6,
  gl.TEXTURE7,
  gl.TEXTURE8,
  gl.TEXTURE9,
  gl.TEXTURE10,
  gl.TEXTURE11,
  gl.TEXTURE12
];

export const FLOAT_BYTE_SIZE = 4;
